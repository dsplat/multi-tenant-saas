我建议把这份文档作为项目的第一份 AI 规范，路径建议：

/docs/ai/01-HUMAN-DISPATCHER-WORKFLOW.md

它不是介绍工具，而是定义团队协作方式。等以后 OpenCode、MimoCode、Qoder 支持 Headless 时，这份文档几乎不用改，只需要把 Human Dispatcher 换成 AI Dispatcher。

⸻

Human Dispatcher Workflow（Phase 1）

Project: Multi Tenant SaaS Framework
Version: v1.0
Status: Draft

⸻

一、设计目标

当前（2026），绝大多数 AI Coding Agent（Claude Code、MimoCode、Qoder、OpenCode）虽具备 Agent 能力，但仍依赖人工启动、输入、确认、查看。

本项目第一阶段采用：**Human Dispatcher 模式**

目标不是无人值守，而是建立稳定、可重复、可演进的 AI 协作流程。

未来当 Agent 支持 CLI、Headless、API 后，只需替换 Dispatcher，Workflow 保持不变。

⸻

二、核心思想：多 Agent 分工协作

AI 不是一个人，是一个团队。

```
需求 → Claude(规划) → Qoder(开发) → Claude(Review) → MimoCode(修复) → Claude(二审) → Done
                       ↑____________ Human Dispatcher 调度各环节 ____________↑
```

**关键约束：**
- 任何 Agent 不允许直接与其它 Agent 通信
- 所有 Agent 通过共享的任务状态进行协作
- Human Dispatcher 是唯一的协调者

⸻

三、团队组成与职责

**Claude（Architect / Reviewer）**

职责：
* Sprint Planning / Roadmap
* Task 拆分与细化
* Architecture 评审
* Code Review（评估不修改）
* Merge Approval

禁止原则：
* 不做大规模业务开发
* 不参与 Debug

---

**Qoder（Developer）**

职责：
* Feature 开发
* API / Component / Store / Router / CRUD
* 跟随 Task 范围开发

禁止原则：
* 不修改 Architecture
* 不修改无关模块或目录结构
* 不自行 Review（必须等 Claude）

---

**MimoCode（Refactor Engineer）**

职责：
* Bug Fix（基于 Review 意见）
* ESLint / Type Error 修复
* 代码清理与重构
* 性能优化

禁止原则：
* 不新增需求
* 不修改产品设计
* 不改变 API 契约

---

**OpenCode（Workflow Assistant）**

当前职责：
* 协助生成 Task / 拆分 Task
* 协助 Build / Test
* 生成报告与检查清单

未来升级：
* 成为 AI Dispatcher，接管 Human 的调度职能

---

**Human（Dispatcher）**

职责：
* 阅读 Task 与 Review 状态
* 决定下一阶段执行者
* 传递上一阶段输出给下一阶段
* 更新任务状态
* 检查 Definition of Done

禁止原则：
* 不参与开发、Review、设计
* 不修改代码逻辑
* 只是流程管理者

⸻

四、任务生命周期（精简版）

所有 Task 必须拥有状态。State machine 如下：

```
NEW → PLAN → READY → DEV → REVIEW ↔ FIX_REQUESTED → TEST → DONE
                       ↓
                      BLOCKED
```

**状态定义：**

| 状态 | 含义 | 执行者 | 后续 |
|------|------|--------|------|
| NEW | 新需求，未规划 | Human | → PLAN |
| PLAN | 已拆分成具体 Task | Claude | → READY |
| READY | 任务完整清晰，可开发 | Human | → DEV |
| DEV | 开发中 | Qoder | → REVIEW |
| REVIEW | 等待 Claude 评审 | Claude | → FIX_REQUESTED or DONE |
| FIX_REQUESTED | Review 有问题，需修复 | MimoCode | → REVIEW |
| TEST | 最终测试确认 | Human | → DONE |
| BLOCKED | 被其他 Task 或外部依赖阻塞 | Human | → READY (解除阻塞后) |
| DONE | 完成，已提交 | Human | ✓ |

**关键约束：**
- Review 最多循环 3 次。第 4 次判定为架构问题，需要 Claude 重新拆分 Task
- 如果 BLOCKED > 48h，需要 escalate 或拆分

⸻

五、标准工作流程

**案例：完成 Auth 模块**

```
1. Human: 提出需求 "完成 Auth 模块"
   Status: NEW → PLAN

2. Claude: 拆分成 4 个 Task
   - Task-001: Login API
   - Task-002: JWT Middleware
   - Task-003: Token Refresh
   - Task-004: Permission Check
   Status: PLAN → READY

3. Human: 选择 Task-001，分配给 Qoder
   Prompt: "完成 Task-001。只允许修改 Auth 模块，完成后输出修改说明。"
   Status: READY → DEV

4. Qoder: 完成开发
   Status: DEV → REVIEW

5. Claude: Review 代码
   输出: Review Report（包括 Architecture/Style/Security/Performance/Bug）
   结果: 发现 3 个问题（Null Risk, Naming, Duplicate Code）
   Status: REVIEW → FIX_REQUESTED

6. MimoCode: 根据 Review 意见修复
   Prompt: "根据 Review 修复这 3 个问题，禁止新增需求。"
   Status: FIX_REQUESTED → REVIEW

7. Claude: 二审，确认修复正确
   Status: REVIEW → TEST

8. Human: 确认 Definition of Done 满足（Build/Type/Lint/Test/Docs/Commit）
   Status: TEST → DONE
```

**关键决策点：**
- REVIEW 有问题 → FIX_REQUESTED（最多 3 轮，第 4 轮 escalate）
- BLOCKED 超过 48h → 需要优先级评估或拆分
- DEV 超过预估时间 2 倍 → Claude 重新审视 Task 范围

⸻

六、异常流程处理

**Case 1: Review 反复失败（循环 > 3 次）**

```
现象: FIX_REQUESTED → REVIEW → FIX_REQUESTED（重复）
处理:
1. Human 暂停当前 Task，标记为 BLOCKED
2. 通知 Claude: "Task-001 Review 反复失败，可能是架构问题"
3. Claude 重新评估 Task 拆分或提升为 Architecture 问题
4. 决策: 拆细 / 返回 NEW / 或标记技术债
```

**Case 2: DEV 时间超时**

```
现象: DEV 状态下超过预估时间的 2 倍
处理:
1. Human 询问 Qoder: "是否遇到阻塞或超出范围的问题"
2. 若是设计缺陷 → 标记为 BLOCKED，通知 Claude
3. 若是范围蔓延 → MimoCode 协助重构，Human 补充 Task 记录
4. 若无法进展 → 考虑 rollback，拆分为更小的 Task
```

**Case 3: 需求变更**

```
现象: DEV/REVIEW 期间出现新需求或需求变更
处理:
1. 原 Task 维持原样，不停止
2. 新需求作为新 Task (NEW 状态)，排入下一 Sprint
3. 不允许在当前 Task 中临时加需求（破坏 Definition of Done）
```

**Case 4: 关键依赖阻塞**

```
现象: Task-001 需要 Task-002 先完成，但 Task-002 无法进展
处理:
1. 两个 Task 都标记为 BLOCKED，记录依赖关系
2. 优先完成 Task-002
3. 或者评估是否能 mock Task-002 的输出，Task-001 先行
```

---

七、Dispatcher 清单

每次分配 Task 前，Human 应检查：

- [ ] Task 描述清晰（范围、输入、输出、限制）
- [ ] 无其他 Task 依赖阻塞
- [ ] 该 Task 有对应的分支或标签
- [ ] Prompt 包含"禁止"项和"只允许"项
- [ ] 预期完成时间已预估

Task 完成后的转移检查：

- [ ] 前置 Task 已完成或不被阻塞
- [ ] 状态转移符合状态机
- [ ] 输出物已上传到对应目录（.ai/review/ 等）

---

八、推荐目录结构

```
.ai/
├── roadmap.md              # 总体规划
├── sprints/
│   ├── sprint-001.md       # Sprint 目标与 Task 列表
│   └── sprint-002.md
├── tasks/
│   ├── TASK-0001.md        # Task 详细描述
│   ├── TASK-0002.md
│   └── ...
├── review/
│   ├── TASK-0001-review.md # Claude 的 Review Report
│   └── ...
├── reports/
│   ├── sprint-001-report.md # Sprint 完成报告
│   └── ...
└── state.json              # 当前所有 Task 的状态快照
```

**TASK-XXXX.md 模板：**

```markdown
# Task-XXXX: [Feature Name]

## 目标
[明确的输出目标]

## 范围
只允许修改:
- Auth 模块
- routes/auth.ts

禁止:
- 修改 User 模块
- 修改 Database Schema
- 新增 npm 依赖

## 输入与依赖
- 需要 Task-YYYY 先完成

## 预期输出
- 代码提交到 feat/auth-login 分支
- 单元测试覆盖 ≥ 80%
- 更新 docs/auth.md

## 预估时间
- 4 小时

## 状态
- NEW / PLAN / READY / DEV / REVIEW / FIX_REQUESTED / DONE
```

---

九、Git 规范

一个 Task 对应一个 Commit。

```
feat(auth): login api - TASK-0001
feat(auth): jwt middleware - TASK-0002
fix(auth): token refresh edge case - TASK-0003
refactor(auth): extract auth service - TASK-0004
```

禁止：一个 Commit 包含多个 Feature。

⸻

十、Definition of Done

一个 Task 标记为 DONE 前必须满足：

- [x] 代码构建成功
- [x] TypeScript 类型检查无错误
- [x] ESLint 无警告
- [x] 单元测试通过（覆盖 ≥ 80%）
- [x] Claude 二审通过（Review PASS）
- [x] 相关文档已更新
- [x] Git 提交完成
- [x] 不包含 console.log 或 debug 代码
- [x] 没有临时文件或注释的废弃代码

如不满足上述任何条件，**不允许**标记为 DONE。

---

十一、Prompt 模板库

**给 Qoder 的 Prompt：**

```
完成 [TASK-XXXX]: [Feature Name]

【范围】
只允许修改:
- [模块名]
- [文件路径]

禁止:
- 修改其他模块
- 修改 Architecture
- 修改数据库 Schema

【输出】
1. 代码提交到 [branch-name]
2. 单元测试覆盖 ≥ 80%
3. 输出修改说明（包含新增/修改的函数、改动原因）

【要求】
- 不要 Review 代码
- 不要自己做 Type Check（稍后 Claude 会检查）
```

**给 Claude 的 Prompt：**

```
请 Review [TASK-XXXX] 的代码。

【输出格式】
## Architecture
[评价架构设计]

## Code Style & Maintainability
[代码风格、命名、可维护性]

## Type Safety
[TypeScript 类型问题]

## Security
[安全风险]

## Performance
[性能问题]

## Potential Bugs
[潜在 Bug]

## Verdict
PASS / FAIL

如果 FAIL，列出必须修复的问题。
```

**给 MimoCode 的 Prompt：**

```
根据 Review 意见修复 [TASK-XXXX]。

【Review 意见】
[粘贴 Claude 的 Verdict FAIL 部分]

【限制】
- 只修复上面列出的问题
- 禁止新增需求
- 禁止修改 Architecture
- 禁止修改无关代码

【完成后】
输出修复说明（每个问题对应的修复方法）
```

---

十二、监控与报告

**Sprint 完成报告模板：**

```markdown
# Sprint-XXX Report

## 概览
- 预计 Task: 5
- 完成 Task: 4
- BLOCKED Task: 1
- 总耗时: 2 周

## 完成的 Task
- TASK-0001 ✓
- TASK-0002 ✓
- TASK-0003 ✓
- TASK-0004 ✓

## 阻塞情况
- TASK-0005: 依赖 API 未交付，预计 +3 天

## Review 统计
- 平均 Review 轮数: 1.5
- 最多 Review 轮数: 3（TASK-0003）
- Review 反复原因: [总结]

## 性能指标
- 代码覆盖率: 85%
- ESLint 错误: 0
- Type 错误: 0

## 下一阶段计划
- [下一 Sprint 目标]
```

---

十三、未来规划（Phase 2 & 3）

**Phase 2: CLI Dispatcher**

当 MimoCode / OpenCode 支持 CLI 调用时：

```bash
./ai-dispatcher task-assign TASK-0001 qoder
./ai-dispatcher task-status update TASK-0001 REVIEW
./ai-dispatcher task-review TASK-0001 claude
```

Workflow 保持完全一致，只是 Human 的操作从手工 → 脚本自动化。

---

**Phase 3: AI Dispatcher**

当 OpenCode 支持 Headless/API 调度时：

```
Human 提供高级目标 → OpenCode Dispatcher 自动分解 → 自动分配给 Claude/Qoder/MimoCode
                  ↓
            完全自主循环 → 定期向 Human 报告进度
```

此时 Human 只需要：
- 审视高级目标和最终结果
- 处理异常情况（阻塞、冲突）
- 做战略决策

---

十四、核心设计原则

1. **Workflow 高于工具** — 任何工具都可以替换，但流程不变
2. **Task 高于 Prompt** — 清晰的 Task 定义比精妙的 Prompt 更重要
3. **状态驱动** — 系统通过状态流转运作，不靠人工记忆
4. **单一职责** — 一个 Agent 一个角色，不兼职
5. **原子 Commit** — 一个 Task 一个 Commit，不混杂
6. **强制 Review** — 所有修改都必须经过他人（或另一个 Agent）的审视
7. **文档 ≈ 代码** — 文档和代码同步演进，不能滞后
8. **Dispatcher ≠ Executor** — Human 是流程管理员，不是代码执行者
9. **AI ≠ 万能** — 每个 Agent 有明确的禁止项和范围限制
10. **渐进自动化** — 流程设计从一开始就为自动化铺路，不是事后补救

---

## 总结

本项目通过 **Human Dispatcher 模式** 实现多 Agent 协作：

- **清晰的角色分工** 避免混乱和冲突
- **状态机驱动** 保证流程可追溯可重复
- **Template-based Prompt** 降低沟通成本
- **严格的 DoD** 保证质量
- **向自动化演进** 而不是一开始就追求完全自动化

这套系统在 Phase 1（Human Dispatcher）时能稳定运作，Phase 2/3 只是替换 Dispatcher 的实现方式，整个 Workflow 逻辑保持不变。

未来，当 AI 工具逐步开放 CLI 与 API 后，可无缝升级到全自动 AI Team，而无需改变项目结构或开发理念。

我建议下一步（也是整个 AI Team Framework 的第二份文档）不是写 Prompt，而是设计 .ai/ 目录规范。

我会把它设计成类似 Git 一样的”协议”，例如定义 TASK.md、REVIEW.md、BUG.md、STATE.json 的标准格式。这样无论是 Claude、MimoCode、Qoder 还是 OpenCode，都遵循同一套协议。未来即使你把其中任何一个工具替换成新的 Agent，也无需修改整个协作体系。这个我认为会是整个项目最核心的基础设施。