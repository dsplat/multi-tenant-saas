# 系统架构概览

**最后更新**: 2026-06-18

---

## 设计原则

1. **租户隔离优先**: 所有数据操作默认按租户隔离
2. **四重访问架构**: admin/console/app/guest 四层访问控制
3. **模块化设计**: 核心能力 + 可选模块
4. **配置驱动**: 通过配置文件控制行为，无需修改代码

---

## 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                      Nginx 反向代理                          │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐  │
│  │ admin.lyt   │ ai.lyt.com  │ ai.tenant1  │ ai-admin.   │  │
│  │ .com        │             │ .local      │ tenant1     │  │
│  └──────┬──────┴──────┬──────┴──────┬──────┴──────┬──────┘  │
│         │             │             │             │          │
│         ▼             ▼             ▼             ▼          │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              Laravel 中间件层                         │    │
│  │  IdentifyDomain → IdentifyTenant → CheckPermission   │    │
│  └─────────────────────────────────────────────────────┘    │
│                          │                                  │
│                          ▼                                  │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              业务逻辑层                               │    │
│  │  Controllers → Services → Models                     │    │
│  └─────────────────────────────────────────────────────┘    │
│                          │                                  │
│                          ▼                                  │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              数据访问层                               │    │
│  │  TenantScope (全局作用域) + BelongsToTenant (Trait)   │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

---

## 核心组件

### 1. 中间件层

| 中间件 | 职责 | 执行顺序 |
|--------|------|----------|
| `IdentifyDomain` | 识别域名类型 (admin/console/api/app) | 1 |
| `IdentifyTenant` | 识别当前租户 | 2 |
| `CheckPermission` | 权限控制 (角色检查) | 3 |
| `EnsureTenantContext` | 确保租户上下文有效 | 4 |

### 2. 上下文管理

| 组件 | 说明 |
|------|------|
| `TenantContext` | 租户上下文管理器，存储当前租户信息 |
| `TenantConfigStore` | 租户配置存储，支持三级缓存 |

### 3. 数据隔离

| 组件 | 说明 |
|------|------|
| `TenantScope` | 全局作用域，自动添加 `WHERE tenant_id = ?` |
| `BelongsToTenant` | Trait，为模型启用租户隔离 |

### 4. 核心服务

| 服务 | 说明 |
|------|------|
| `IdGenerator` | 16位随机ID生成器 |
| `TenantService` | 租户CRUD管理 |
| `TenantSettingService` | 租户配置管理 |
| `TenantCreditService` | 积分/配额管理 |

---

## 模块系统

```
src/Modules/
├── Domain/          # 域名管理模块
│   ├── Services/
│   ├── Commands/
│   └── Config/
└── SSL/             # SSL证书模块
    ├── Services/
    └── Config/
```

### 模块注册

在 `TenancyServiceProvider` 中注册模块：

```php
public function register(): void
{
    // 注册域名管理模块
    if (config('tenancy.modules.domain.enabled', true)) {
        $this->app->register(\MultiTenantSaas\Modules\Domain\DomainServiceProvider::class);
    }
    
    // 注册SSL模块
    if (config('tenancy.modules.ssl.enabled', true)) {
        $this->app->register(\MultiTenantSaas\Modules\SSL\SSLServiceProvider::class);
    }
}
```

---

## 请求处理流程

```
HTTP Request
    │
    ▼
Nginx
    ├─ 识别域名 → 路由到 PHP-FPM
    └─ 注入 X-Original-Host header
    │
    ▼
Laravel Middleware Stack
    │
    ├─ 1. IdentifyDomain
    │     读取 X-Original-Host + path
    │     → domain_type: admin | console | api | app
    │
    ├─ 2. IdentifyTenant
    │     IF admin → 跳过
    │     ELSE 按优先级解析租户 ID：
    │       URL参数 → Header → 自定义域名 → Cookie → Session → 默认租户
    │     → tenant_id, tenant 注入 TenantContext
    │
    ├─ 3. CheckPermission
    │     检查用户角色和权限
    │     console: 仅 tenant_admin
    │     app: tenant_admin + end_user
    │
    └─ 4. Controller
          业务逻辑处理
    │
    ▼
Eloquent Model
    └─ TenantScope 自动: WHERE tenant_id = {current_tenant_id}
```

---

## 环境配置

### 必需的环境变量

```env
# 应用配置
APP_NAME="Multi-Tenant SaaS"
APP_ENV=local
APP_KEY=base64:xxx
APP_DEBUG=true
APP_URL=http://localhost

# 数据库
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=multi_tenant_saas
DB_USERNAME=saas_user
DB_PASSWORD=your_password

# 域名配置
ADMIN_DOMAIN=admin.lyt.com

# 会话
SESSION_DRIVER=database
SESSION_LIFETIME=120
```

---

## 目录结构

```
multi-tenant-saas/
├── app/
│   ├── Http/
│   │   ├── Controllers/    # 控制器
│   │   └── Middleware/     # 自定义中间件
│   └── Models/             # 业务模型
├── config/
│   ├── tenancy.php         # 框架核心配置
│   ├── id.php              # ID生成器配置
│   ├── domain.php          # 域名配置
│   └── ssl.php             # SSL配置
├── database/
│   └── migrations/         # 数据库迁移
├── src/                    # 框架核心代码
│   ├── Concerns/           # Traits
│   ├── Context/            # 上下文管理
│   ├── Contracts/          # 接口定义
│   ├── DTOs/               # 数据传输对象
│   ├── Enums/              # 枚举
│   ├── Exceptions/         # 异常
│   ├── Helpers/            # 辅助函数
│   ├── Middleware/          # 中间件
│   ├── Models/             # 框架模型
│   ├── Modules/            # 可选模块
│   ├── Scopes/             # 全局作用域
│   ├── Services/           # 服务层
│   └── TenancyServiceProvider.php
└── tests/                  # 测试
```

---

## 技术栈

- **PHP**: ^8.2
- **Laravel**: ^11.0 | ^12.0
- **数据库**: MySQL 8.0+
- **缓存**: Redis (推荐) / Database
- **Web服务器**: Nginx + PHP-FPM

---

**文档版本**: v1.0.0  
**最后更新**: 2026-06-18
