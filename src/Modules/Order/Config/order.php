<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 路由子前缀
    |--------------------------------------------------------------------------
    | 模块路由挂载于 api/v1 之下的子前缀。
    | - 默认 ''：/api/v1/orders、/api/v1/my/orders …
    | - 下游项目可覆盖为既有命名空间（如 scrm 配置 'scrm' → /api/v1/scrm/orders）
    */
    'route_prefix' => '',

    /*
    |--------------------------------------------------------------------------
    | 支付回调共享密钥（C1：pay-notify 防伪造）
    |--------------------------------------------------------------------------
    | pay-notify 请求必须携带 X-Pay-Notify-Key 头且与本配置一致才放行。
    | 未配置（null/空）时拒绝一切回调（安全默认，不留开放面）。
    | 真实支付网关接入后由网关验签链路取代本过渡方案。
    */
    'pay_notify_key' => env('ORDER_PAY_NOTIFY_KEY'),

];
